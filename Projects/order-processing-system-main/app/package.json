{
  "name": "order-processing-system",
  "version": "1.0.0",
  "description": "Order Processing System with Azure Event Hub",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest",
    "docker:build": "docker build -f ../dockerfiles/app.Dockerfile -t oms-app .",
    "docker:run": "docker run -p 3000:3000 --env-file ../.env oms-app"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "mysql2": "^3.6.0",
    "@azure/event-hubs": "^5.10.0",
    "@azure/identity": "^3.2.3",
    "dotenv": "^16.3.1",
    "winston": "^3.11.0",
    "helmet": "^7.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  }
}